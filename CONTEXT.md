# TripMate — Session Context

Read this FIRST, before grepping/exploring the codebase for a new task. It's a
living summary — update it (don't append forever; edit stale lines in place)
at the end of any session that changes architecture, fixes a real bug, or
learns something non-obvious. Full docs (`docs/ARCHITECTURE.md`, `docs/API.md`,
`docs/AGENT_PROMPTS.md`, `README.md`) are the source of truth for anything not
covered here — only go read them if this file doesn't answer the question.

## Stack (one line)

React 18 + Express 4 + MongoDB/Mongoose + Socket.io. Atlas (chat agent) runs
OpenRouter → Groq → NVIDIA NIM×2 fallback chain; Gemini (`gemini-3.5-flash-lite`)
handles separate AI utility calls (weather/travel-hacks/journal); OpenAI wired
but unfunded. Deployed on Render: https://tripmate-ylt6.onrender.com

## Where things live

- `server/agent/agentLoop.ts` — Atlas chat loop + provider fallback
- `server/agent/tools/handlers/` — Atlas tool implementations (one per REST-equivalent action)
- `server/agent/multiAgent/MasterOrchestrator.ts` + `agents/` — separate non-chat pipeline (SuggestionAgent, ItineraryAgent, BudgetAgent, MapAgent, PackingAgent, JournalAgent, HeroImageAgent), tracked via `AgentJob`
- `server/controllers/` — REST handlers; most have an Atlas tool-handler twin doing the same mutation
- `shared/schema.ts` — single source of truth for Zod + Mongoose models
- `server/config.ts` — authoritative env var list
- `client/src/hooks/useUserLocation.ts` / `usePlaceSuggestions.ts` / `useTripDestinationCoords.ts` + `client/src/components/PlaceSearchDropdown.tsx` — shared place-search-with-suggestions stack (debounced, location-biased). Used by ActivityFormDialog, TripPlanner origin/destination, Emergency, OfflineMaps explore search, Weather (client-side distance sort, its dropdown predates this and has its own keyboard nav). Landing's hero destination search intentionally has no location bias (no "near me" for "where do you want to go").
- `client/src/lib/time.ts` — shared "HH:MM AM/PM" ⇄ minutes helpers + `nextAvailableTime()` (stacks a new activity after the day's last one instead of a fixed default time)

## Non-obvious facts that cost real debugging time to learn

- **ID types are inconsistent by design**: `User._id` and most models use nanoid **strings**. `Trip._id` is a real Mongo **ObjectId**. A model with `userId: Schema.Types.ObjectId` when the actual value is a nanoid string silently CastErrors on every query — killed two whole features (Suggestions, Job History) before it was caught.
- **Trip collaborative writes are atomic-op-based, not `.save()`** (fixed 2026-08-31/09-01, commit `9144e4b`) — `expense.controller.ts`, `itinerary.controller.ts`, and their Atlas tool-handler twins use `$push`/`$pull`/`$set` with `arrayFilters` to avoid whole-document read-modify-write races. `modifyItineraryHandler.ts` is the one exception (needs the full itinerary read for its feasibility check) — it uses optimistic concurrency (compare-and-swap on `updatedAt`) instead.
- **Google Places Photo URLs must never embed `GOOGLE_API_KEY` client-side** — always go through `/api/v1/places/photo?ref=<photo_reference>` (server-side proxy, `redirect: "follow"` since Google 302s). This leaked 4 separate times across the session in different call sites before all were found — grep for `maps.googleapis.com/maps/api/place/photo` before shipping anything that touches Places.
- **Auth is cookie-primary**, not Bearer-primary — the SPA never sends an `Authorization` header; it's accepted as a fallback only. CSRF middleware skips the check when there's no auth cookie or when a Bearer header is present.
- **Avatars are base64-in-Mongo**, not disk-uploaded — Render's disk is ephemeral.
- **Maps use OpenStreetMap tiles**, not CartoDB (CartoDB now requires a paid key). Dark mode is a CSS filter on the Leaflet tile pane, not a second tileset — don't add one, it doubles OSM request volume.
- **QA/test accounts**: pattern is `@example.com` or `@tripmate.dev`, but real confirmed accounts also exist at other domains — always check with the user before bulk-deleting. `claude.code@tripmate.dev` is a persistent, intentional exception (not junk). Broad regex-scoped bulk deletes against prod trigger the safety classifier; itemized/exact-ID deletes don't.
- **Design system** is "Night Atlas / Passport & Visa Stamp" — dark-by-default via `:root` tokens in `client/src/index.css` (light is the `.light` override, not the reverse). Only 5 named ink tokens exist (`--amber`, `--explorer-blue`, `--explorer-blue-deep`, `--emerald-horizon`, `--ios-red`) — not enough to cleanly token-ize a 7-8 way category legend (e.g. TripMap's activity-type marker colors, day-route colors). Don't force those onto the existing palette; that's a real design-system gap (needs a deliberate new-tokens decision), not a mechanical literal-hex-to-token swap.
- **Never write `[var(--x)]/N` (Tailwind's arbitrary-value opacity modifier on a hex-valued CSS custom property) — it silently compiles to nothing.** Tailwind needs a space-separated RGB channel list to build `rgb(var(--x) / N%)`; a `var()` that resolves to `#rrggbb` can't be parsed that way and the whole utility class is dropped at build time (not even a broken rule — literally absent from the output CSS). Every ink token that's ever used with an opacity modifier has an `--x-rgb` companion in index.css (`--amber-rgb`, `--forest-rgb`, `--explorer-blue-rgb`, `--ios-red-rgb`, `--emerald-horizon-rgb`) — use `[rgb(var(--x-rgb)/N%)]` instead. `[hsl(var(--x))]/N` (the shadcn-style HSL tokens: `--border`, `--muted-foreground`, `--card`, etc.) is unaffected and works fine as-is. This shipped broken across 8 files for an unknown span before a code-review agent caught it (2026-09-02, commit `bb94bf8`) — grep for `\]/\d` next to a bare `var(--` before assuming a hover/tint state is working.
- **Production auth endpoints (`/auth/signin`, `/auth/signup`) are rate-limited at 5 requests per 15-minute window**, separate from the general API limiter (100/15min) and far stricter than it looks from casual testing — a live-verification script that does more than ~4 signups/logins against production in a short span will self-lock for the rest of the window, and checking `ratelimit-remaining` itself counts as a request against the same window. Reuse one signed-in session (navigate/resize within it) across everything a live QA pass needs, instead of a fresh signup or login per page/viewport.
- **Playwright/Radix testing gotcha**: shadcn's `TabsTrigger` doesn't reliably expose a `[value="x"]` DOM attribute to query against — use `[role="tab"]` + a text match instead. And a raw `element.click()` via `page.evaluate()` does NOT activate a Radix Tabs/similar primitive (Radix listens for a real pointer-event sequence, not just a synthetic `'click'`) — use Playwright's own `.click({ force: true })` on a real element handle, not a JS-dispatched one.

## Known open issues (not yet fixed, don't re-discover from scratch)

- **The OpenAI account backing `OPENAI_API_KEY` is out of credits** (confirmed live 2026-09-02, literal 429 "no credits remaining" from OpenAI's own billing page). Weather was fixed to fall over to Gemini on a runtime OpenAI failure, not just a missing key, but the same "if (this.openai) {...} else {gemini}"-with-no-runtime-fallback pattern exists at several other `AiUtilitiesService.ts` call sites (itinerary generation, translation, journal recap) — those will fail the same way until either the account is topped up or each site gets the same runtime-fallback treatment weather got. Not fixed everywhere; only what was reported/reproduced this round.

## Non-obvious facts (cont.)

- **`Object.entries(req.body)` / `Object.assign(doc, req.body)` into a Mongo `$set` or a Mongoose document is a recurring vulnerability pattern in this codebase** — found 4 times (updateExpense, updateActivity, createPackingList, updatePackingList) before all were fixed with an explicit field allowlist. Grep for `Object.entries(` / `Object.assign(` near a `.save()` or `$set` before adding a new mutation route.
- **`validate.ts` now actually strips unknown body/query/param keys** (fixed 2026-09-01) — previously it ran `schema.parseAsync()` for its side effect of throwing on bad input but discarded the parsed result, so every Zod-validated route in the app accepted arbitrary extra keys through validation. If a controller ever seems to need a field the schema doesn't declare, add it to the schema — don't work around this middleware.

## Recent work log (most recent first — trim entries older than ~4-6 weeks)

- **2026-09-03**: Live-reported (screenshot): trip hero banner looked visibly blurry. Root cause: `/api/v1/places/photo` (the same-origin proxy every Google-Places-sourced image goes through) hardcoded `maxwidth=400` for every caller — fine for small Places-tab search-result cards, but the hero (`TripDetail.tsx`, `w-full` up to ~900px, `object-cover`) was stretching that same 400px source across its full width. Confirmed inconsistent with the hero's own Wikipedia-thumbnail fallback for the identical `imageUrl` slot, which already asks for `pithumbsize=1200`. Fixed narrowly: `/photo` now accepts an optional `?w=` (clamped 1-1600, default unchanged at 400), and only the hero-image call site (`trips.controller.ts`) passes `w=1200`; Places-tab cards and the landing page's destination thumbnails are untouched. Existing trips' already-persisted (blurry) `imageUrl` picks up the fix via the hero's own existing refresh button, no migration needed. Live-verified: `w=400` vs `w=1200` on the same real photo_reference returned 38KB vs 291KB (~7.6x), no-`w=` default confirmed unchanged. Commit `06e30fb`.
- **2026-09-03**: Edge-case + regression-spec pass for the f61bcf0 "fix all" batch. Probed 6 behaviors with real Playwright against production, hunting failure modes the original happy-path pass missed. Findings: Bug 1 (coords) — REST endpoint gracefully accepts coord-less activity (status 201) and UI renders the row without crash; no View-on-Map button appears for the activity (graceful absence confirmed). Bug 2 (prefs) — `[]` and `undefined` both fall back correctly (`[].length===0`, `!0===true`); conflicting Vegetarian+BBQ prefs both persist (Mongoose does NOT drop either array). Bug 3 (system prompt) — `finish-what-you-start` and `MUST respect them` rules confirmed present in systemPrompt.ts; live Atlas behavior verified in the original f61bcf0 session. Bug 4 (500 vs empty) — page.route() to inject 500 on hotel search: error banner appeared with the expected "search failed" copy; retry button present; unblocking the route and clicking retry cleared the error state after ~8s (Render free-tier latency, not a bug). Other categories (Tourist Spots) unaffected by the hotel-only block. Bug 5 (View-in-Places targeting) — second View-in-Places click changed banner text to the new restaurant name (distinct from the first); show-all link cleared the banner. Bug 6 (ad-hoc marker) — custom-marker count was 9 after the first click and still 9 after the second click on a different result (no accumulation). Written `tests/e2e/fixall-batch.spec.ts` (9 tests, all 6 bugs + hygiene) and `playwright.config.ts` at repo root. Post-write: fixed 4 TypeScript errors the node-heredoc shell-quoting chain introduced (broken CSS attribute selector → single-quote wrapper, broken JSON string in route.fulfill body, wrong `vipBtns` type annotation, unused `type Page` import). Spec typechecks clean (`tsc --noEmit`, zero errors). Account hygiene: no throwaway trips left; confirmed via authenticated GET /api/v1/trips → only Manali and Udaipur remain. **Separately, a parallel code-reviewer agent audited the same batch and found one real remaining bug**: `TripMap.tsx`'s ad-hoc-marker cleanup only ran inside the `if (!marker) {...}` branch, so it never fired when the NEXT focus target happened to have a real itinerary marker — a Places-tab place viewed via the ad-hoc marker, followed by "View on Map" on an itinerary activity elsewhere, left the first ad-hoc pin stuck on the map permanently (no popup, no way to dismiss). Fixed by hoisting the cleanup to run unconditionally at the top of the focus effect. Also fixed a minor inefficiency the same review caught: `tryResolveCoords` (modifyItineraryHandler.ts) only guarded on `act.latitude`, not `act.lat` (the app's canonical field name elsewhere) — an activity that already had `lat`/`lon` but not `latitude` got needlessly re-geocoded on every edit; now guards on both. New backend regression suite `tests/backend/fixall-batch.test.ts` (17 tests, real handler imports + real test-DB) pins down all of Bug 1/2's behavior including this fallback-condition edge case. Typecheck/build/full suite clean throughout.
- **2026-09-03**: "Fix all" batch closing out the walkthrough-observed bug list from earlier the same day, plus one more live-reported mid-batch. (1) `modifyItineraryHandler.ts`: `resolveCoordinates(act.address)` was called with only 1 of its 2 required args in all 3 action branches — `destination` was `undefined`, geocoding silently failed, Atlas-added/edited activities lost their View-on-map icon. Fixed via a `tryResolveCoords` helper using the correct `(name, destination)` signature; also normalizes shouting-case/stray-punctuation titles Atlas sometimes returns. (2) `tripHandler.ts`: the `get` action Atlas calls every turn never included `cuisinePreferences`/`dietaryPreferences` at all — a vegetarian user's stated preference had zero effect on restaurant suggestions. Now surfaces trip-level prefs, falling back to profile-level when the trip has none. (3) `systemPrompt.ts`: added an explicit rule that suggestions must respect the now-surfaced preferences, plus a "finish what you start" rule — Atlas was observed stopping after a lookup tool call without following through to the write. (4) `TripDetail.tsx` place-search queries: a failed search and a genuinely empty result both silently returned `[]`; now throws on `!r.ok` and shows a distinct "search failed, try again" state. (5) View→Places targeting: clicking a restaurant/hotel activity's View button landed on a generic category browse that often didn't contain that place; now threads the activity's name through to a name-specific search, with the matching card highlighted and a "showing results for X / show all" banner. (6) TripMap "View on Map" from the Places tab flew to the right coordinates but showed no pin and no name — markers are only ever built from itinerary activities, so a searched-but-not-added place had none to find. Now creates an ad-hoc marker with the place's name when no real marker exists at that location, cleaned up automatically if the place is later actually added to the itinerary. Typecheck/build clean, 128/129 tests pass (1 pre-existing OpenAI-credits failure, unrelated). **Live-verified end-to-end** (real Playwright + real backend calls against production, `claude.agent@tripmate.dev`, several fresh throwaway trips created and deleted): Atlas real runs added "Lunch at Ratnika - A Vegetarian Restaurant" / "Lunch at Govindam Retreat" / "Lunch at Laxmi Misthan Bhandar (LMB)" to different trips — all genuinely vegetarian spots (proves preference-surfacing), correctly-cased titles, real `latitude`/`longitude` populated (proves the coords fix), `modify_itinerary` actually called every run (proves task-completion). Places→View banner + highlighted card confirmed live. Map-pin fix confirmed live via a hotel search result never added to any itinerary: clicking its "View on Map" produced a real Leaflet marker + a popup reading exactly "Rajasthan Palace - A Heritage Boutique Hotel in Jaipur" (the ad-hoc-marker path, not the itinerary-marker reuse path — verified by deliberately picking a place with no existing itinerary marker). One caveat found, not part of this batch's scope: Atlas's own reply sometimes lists trips from its persistent chat memory rather than a fresh `list_trips` call and can be a turn stale (confused two already-deleted throwaway Jaipur trips with the live one) — cosmetic to this account's session history, not a data-correctness bug (every actual write landed on the correct trip), flagged for awareness only. Commit `f61bcf0`.
- **2026-09-03**: Fixed 2 mobile UI bugs from a live-Playwright-verified 375px QA report. (1)
  `TripDetail.tsx`'s header Edit/Delete buttons: Delete would wrap onto its own line at 375px,
  isolated from Edit — DOM measurement showed the two buttons are actually the same computed
  size (40x36 vs 38.25x36); the "oversized red circle" was Delete orphaning alone at the row's
  far-left edge with nothing beside it for scale, not a real size difference. Root cause: Edit
  and Delete were two separate children of the same `flex-wrap` container (alongside
  PresenceBubbles/CollaboratorManager), so the wrap algorithm could split them across lines.
  Fixed by grouping Edit+Delete into their own flex sub-div (mirroring the existing
  PresenceBubbles+CollaboratorManager grouping pattern two lines above) so they always wrap as
  a pair. (2) Packing page season tabs (`client/src/pages/app/packing/index.tsx`) clipped to
  "Sum…"/"Autu…" at 375px — each icon had a hardcoded `mr-2` stacked on top of the
  TabsTrigger's own flex `gap-1`, double-spacing icon-to-label inside an already-tight ~84px
  grid column. Removed the redundant margin, tightened mobile padding/gap, and swapped
  `truncate` for `whitespace-nowrap` (so any future overflow is visible/obvious instead of
  silently ellipsis-clipped). Typecheck/build/full unit suite (17 files/92 tests) clean.
  Live-verified against production post-deploy (real Playwright, `claude.agent@tripmate.dev`
  session reused across checks to respect the auth rate limit): confirmed via both DOM
  bounding-box measurement and screenshots at 375/430/768px — Edit+Delete same row at all three,
  all 4 season labels render with zero clipping (`scrollWidth === clientWidth`). Commit `a4b9ba6`.
- **2026-09-03**: Two parallel code-reviewer agents against the itinerary-dedup/location-picker fixes and the Import My Plan upgrade, each asked to end with runnable test scripts. Found and fixed 4 real bugs the earlier sessions had missed: (1) the dedup overlap ratio used `Math.min(sizeA,sizeB)` as its denominator, so one shared word was enough to match against a much larger set — "National Museum" and "National War Memorial" false-merged at 1/min(1,3)=1.0; fixed to `Math.max`. (2) `distinguishingTokens` unconditionally stripped a generic feature word ("fort", "museum"), so a real landmark that's JUST destination+genericWord ("Agra Fort", "Jaipur Museum") lost its only signal and got misclassified as a repeat; fixed to only strip when 2+ tokens survive. (3) Import My Plan's cache hash omitted `startDate` while the AI embeds concrete per-day dates for whatever date it was given — a cached Oct-1 trip would get served to a Nov-15 request, wrong dates and all; fixed by adding it to the hash (and fixing a related inconsistency: the hash used a raw `Date.toString()` while the AI prompt used an ISO date string for the same value — now computed once, reused both places). (4) `getBudgetBracket` was always called with `days=1`, so a 10-day ₹30,000 trip (₹3,000/day, "mid") read as "premium"; now estimates day count from "Day N" occurrences in the pasted text. Added `tests/backend/dedup.test.ts` (31 tests) and `tests/backend/import-plan.test.ts` (22 tests) from the agents' own scripts, adapted to this repo's real helpers — a couple of the agents' own assumptions were wrong too (an invalid `travelStyle` enum value, a cache-hash date-format mismatch that made two tests fall through to a real slow AI call) and got fixed in the process. Typecheck/build clean, full suite 86/87 (the one failure is the already-documented pre-existing OpenAI-credits flake, isolated-reconfirmed). Commit `5052cad`.

- **2026-09-03**: "Import My Plan" upgrade (Steps 1-4 of a 5-step plan; Step 5, SSE streaming, deliberately deferred — see the plan/estimate given before building). Adapted from a Claude API + Supabase spec to this codebase's real stack (Gemini/NVIDIA/OpenAI fallback chain, MongoDB) per explicit go-ahead to follow intent over literal tool names. **Step 1**: `parseScheduleSchema` (server-side floor: 20-3000 chars, a capitalized-word location heuristic, groupSize≥1, start date not in the past — previously client-only), a real per-user 3/min `importPlanLimiter` (was the shared 20/min per-IP `aiLimiter`), inline form errors surfacing the actual server rejection reason. **Step 2**: richer prompt/schema for `parseSchedule` — time-blocked days, transit-day handling, time-sensitive anchoring, 15min travel buffers, budget-bracket-aware suggestions, last-day-per-city `departureReminder`, `whyVisit`/`localTip` per activity, one retry with a shorter prompt on invalid JSON, runs through `finalizeAttractionDedup` (the Ameenpur-session fix) before returning. Field names kept consistent with the rest of the app's itinerary shape (not the spec's literal `arrival_time`/`cost_inr`) so an imported plan works with every existing itinerary feature (Budget, TripMap, ItineraryManager) with no separate code path. Added a day-level `.catchall(z.any())` to both `createTripSchema` and `insertTripSchema` (mirroring the existing activity-level one) so the new day-level fields actually survive persistence — confirmed live: a created trip's day 3 `departureReminder` round-trips through GET intact. **Step 3**: `ImportPlanCacheModel` (SHA-256 hash of sanitized-input+groupSize+budgetBracket+currency, 7-day TTL) and `ImportPlanRequestLogModel` (userId/cacheHit/durationMs/aiModel/tokensUsed — the last only populated where the OpenAI fallback SDK actually returns it), both fire-and-forget writes that never block the response. Live-verified: a repeat request returned in 1.6s vs the first call's 7.4s, same content. **Step 4**: new `ImportPlanPreview` component — review-before-create screen (matches the "Let AI Plan" flow's own Save-Trip-after-review pattern) with collapsible day cards, amber-left-border time-sensitive styling, a red departure-reminder banner, booking-priorities/packing-notes sections (preview-only, not persisted — matches how "Let AI Plan"'s own packingList/safetyTips are already treated). Caught before shipping: the first draft used a `sticky bottom-4` action bar, which risks sitting on top of this app's fixed mobile bottom nav (Layout.tsx, `bottom-3`) — same overlap-bug class already documented below; replaced with the plain in-flow button row the AI-plan review screen already uses. Live-verified end-to-end via real Playwright (signup → parse → preview renders without auto-creating → day cards collapsed by default → departure banner + time-sensitive styling both visible on expand → zero horizontal overflow at 375/430/768/1024, screenshotted → Create This Trip → real trip created with the itinerary and departureReminder intact). Typecheck/build/full suite (23 backend + 11 frontend) clean throughout. Commits `97ce895`, `7cbb636`, `e8da5b4`.
- **2026-09-03**: Live user report: AI itinerary for a small destination (Ameenpur) repeated the same lake across all 3 days under cosmetically reworded titles ("Ameenpur Lake" / "Ameenpur Lake View Point" / "Ameenpur lake fish feeding area"). Two fix attempts targeting `AiUtilitiesService.planTrip`'s multi-agent path (DraftingAgent prompt + a `groundItineraryWithRealPlaces` backstop) shipped and were re-verified live — and still failed, because **the actual code path never runs the multi-agent pipeline for this destination at all**: the response's `notes` field (only checked on the 3rd attempt) read "Generated by Smart Fallback," meaning `zTripPlan`'s strict Zod schema (no optional fields on `routeFromPrevious`/`entryFee`) throws for this destination and every request falls through to `generateFallbackTrip` — a separate, simpler generator neither fix touched. Its real bug: sequential indexing into one `searchPlaces("<dest> tourist places")` array per day with no dedup, and Google Places genuinely returns multiple distinct Place IDs for facets of the same landmark (the lake itself, its viewpoint, its walking track are 3 separate real Google records). Fix: added `finalizeAttractionDedup` — destination-name-aware (so a real different landmark starting with the same town name, e.g. "Ameenpur Kaman", doesn't false-positive) — called unconditionally in _both_ code paths, live-verified against the exact reported destination post-fix: 3 genuine duplicates rewritten to "Relax / free time at Ameenpur Lake", 2 genuinely distinct places (Kaman, Walking Track) correctly left alone. **Lesson logged as CLAUDE.md rule 5**: think from the traveler's side before the code's — the first prompt-level fix draft told the model to invent day-trips to nearby towns to pad out a small destination, which a real user immediately (and correctly) called out as presumptuous scope creep nobody asked for; replaced with "give the real spot more time" instead. Also fixed in the same round: TripPlanner's origin/destination search shared the same unrestricted place-picker every other search bar uses, so typing "Vijaya" returned a hospital and a dairy parlour instead of cities — first attempt (`type=locality` on Google Text Search) was verified as a no-op directly against Google's raw API and replaced with Places Autocomplete (`types=(cities)`), which actually enforces it. Commits `aec2ad6`, `8af4e9a` (superseded), `b586a85`, `6eceb6f` (superseded), `e8f7bf7`, `4267149`, `7d22979`.
- **2026-09-03**: Live-verified (real Playwright + real backend API calls, per explicit user instruction to not declare anything done without it) the three most-recently-shipped features: cuisine/dietary trip preferences, per-trip Atlas chat persistence, and type-based itinerary View routing (restaurant/cafe/accommodation → Places tab, sightseeing → Map tab). Atlas persistence and the View-routing confirmed clean on first honest attempt (reload kept the message; both View buttons correctly flip `data-state=active` on the right tab, screenshotted). Along the way found and fixed **two real, previously-unnoticed bugs in trip creation itself**, unrelated to any single feature but blocking proper verification of all three: (1) `createTripSchema` (`server/schemas/trip.schemas.ts`) only ever declared the handful of fields the earliest version of trip creation had — `validate()` replaces `req.body` wholesale with a schema's parsed output, so `itinerary`, `notes`, `costBreakdown`, `transportMode`, and both cuisine/dietary arrays were silently stripped to undefined on **every real `POST /api/v1/trips`**, confirmed via a canary field live against production. A user generating an AI itinerary in TripPlanner and hitting Save landed on a trip with zero activities, no error anywhere — this was not new, TripPlanner has sent `itinerary` in that payload for a while. (2) `TripPlanner.tsx` never included `cuisinePreferences`/`dietaryPreferences` in either `createTrip` payload (only forwarded them to `/generate-itinerary` to steer the AI draft) — so `trip.cuisinePreferences`, which `TripDetail`'s Places-tab restaurant search reads, was always empty for a real saved trip. Fixed both; re-verified end-to-end live: a trip created with `dietaryPreferences: ["Vegetarian"]` surfaced "Kailash Parbat - Pure Vegetarian Restaurant" as its top Places-tab result. Typecheck/build/`trips.test.ts` (4/4) clean. Commit `f2d177f`. Also confirmed (not a bug): the real trip-detail route is `/app/trips/:id`, not `/trip/:id` — a bare `/trip/:id` 404s ("We lost this page"); worth remembering for any future test script.

- **2026-09-03**: New feature — a "View on map" button on each itinerary activity that has coordinates, switching to the Map tab and flying the camera to that activity's marker (opens its popup too). Building it surfaced two real, unrelated bugs, both fixed and live-verified alongside: (1) **manually added/edited activities were silently losing most of their fields** — `addActivitySchema`/`updateActivitySchema` only allowed `{time, title, location, notes, latitude, longitude}`, but `ActivityFormDialog.tsx` sends `{time, title, placeName, address, type, cost, entryFee, duration_minutes, lat, lon, from, to, notes}` — since `validate.ts` strips unknown body keys (a real, separate fix from 2026-09-01), every field not on that narrow list was discarded on every manual add/edit: no coordinates meant no map marker at all, no `type` meant it always fell back to the generic bucket, no `cost`/`entryFee` meant budget tracking never saw it. Widened the schema to the full field set and renamed the coordinate fields to `lat`/`lon` (matching `IItineraryActivity`, TripMap's marker code, and every AI-generated activity — confirmed nothing server-side ever read the old `latitude`/`longitude` names). (2) **the flyTo/popup never fired on the first switch to the Map tab** — the focus effect ran on mount before the Leaflet map instance existed (it's created in a separate effect gated on an async destination-geocode fetch), bailed out silently, and never got a second chance since the focus-target object reference doesn't change on its own; recurred on every tab switch since TripMap remounts each time (Radix Tabs' default). Fixed by adding `coords` (real state) to that effect's dependency array. Confirmed end-to-end via real signup → trip → activity → itinerary → click → map tab active, camera flew to the pin, popup opened with the exact right content. `TripDetail.tsx`'s previously-uncontrolled Tabs is now controlled (`value`/`onValueChange`) to make the tab switch possible at all. Commits `eb3b056`, `c433f86`, `eaa618d`.
- **2026-09-02**: Full real-user QA pass (2 parallel agents walking every page live, no code reading) found 3 real bugs and several polish issues; all fixed and live-verified. **Trip page permanently breaks on reload** ("Trip Not Found" on refresh/direct link, reproduced independently twice) — `TripDetail.tsx`'s `fetchTrip` fired unconditionally on mount instead of waiting for `isAuthenticated` (every other query in that file already gated on it), then never retried after one failure, and the axios `ApiError` is a plain object so `instanceof Error` always failed, collapsing every cause into one generic message. Fixed: gate on auth, auto-retry once, detect the real error shape, differentiate the message (sign-in vs not-found vs connection issue). Confirmed via direct HTTP (3 consecutive curl GETs, same cookie) that the backend was always fine — 100% client-side race. **Weather always showed fake "estimated" data** for any city, even Paris — root-caused through live diagnosis (a temporary `_debug` field surfaced the actual caught errors, since Render logs aren't accessible from here): `OPENAI_API_KEY` is configured but that account is **out of credits** (confirmed via the literal 429 "no credits remaining" text), and `weather()`'s Gemini fallback only ever triggered on a _missing_ key, never a runtime failure — same "if (this.openai) {...} else {gemini}" pattern exists at several other call sites in `AiUtilitiesService.ts` (itinerary gen, translation, journal recap) and would fail the same way; only weather was touched since only it was reported. Also found and fixed: the model's JSON used `temperature_c`/`humidity_percent`/`wind_kph` instead of the `temperature`/`humidity`/`windSpeed` every other branch produces and `WeatherWidget.tsx` reads — would have rendered "NaN°" even after the Gemini fallback started working; tightened the prompt's field-name spec and added a normalizer. A silent Open-Meteo-fallback swallow (`if (ok) {...}` with no `else`, so a non-ok response fell through with nothing thrown or logged) was fixed alongside. Commits `a357da7`, `9b80407`, `6422fa6`. Polish pass in the same round (Feedback.tsx required-field highlighting mirroring TripPlanner's existing pattern, currency/weather/tablet-planner mobile text clipping, geolocation-denial wording, OfflineMaps' collapsed-sheet not signalling it holds search) in `31ed0c1`. One reported item (Emergency defaulting to Lahore) was live-reproduced with geolocation genuinely denied and found to be a false positive — a test-harness geolocation-mock artifact, not an app bug; not touched.
- **2026-09-02**: Dispatched code-reviewer + design-auditor agents against this session's search-suggestions/time-fix/color-token commits (per CLAUDE.md's "run agents for reports" rule). Found a real, pervasive, previously-invisible bug: Tailwind's `[var(--x)]/N` opacity-modifier syntax silently emits **no CSS rule at all** when `--x` is a hex-valued custom property (verified against actual Tailwind CLI output) — not a rendering glitch, the class does nothing. Had been shipping across Journal.tsx, JournalDetail.tsx, Landing.tsx, Tools.tsx, ItineraryManager.tsx, RecapCard.tsx, ChatInput.tsx, ChatMessageList.tsx (19 call sites, all pre-dating this session's audit). Fixed by adding `--x-rgb` space-separated-channel companions to every affected token in index.css and switching call sites to `[rgb(var(--x-rgb)/N%)]`, which does compile — see "Non-obvious facts" below for the durable rule. Also fixed from the same audit: a WCAG-failing ~1.8:1 text contrast on TripMap's active route-toggle button (switched to solid-fill + white text), PlaceSearchDropdown's `shadow-2xl` → `var(--shadow-card)` token, ActivityFormDialog's suggestion dropdown being clippable by the dialog's `overflow-y-auto` on short viewports (restructured so only the fields below the search box scroll), an unbounded `pageSize` on `/places/search` (clamped to Google's own 20/page ceiling), and a silently-swallowed fetch failure in `usePlaceSuggestions` (now logs a `console.warn`). Typecheck/build/test (76/76) clean. Commit `bb94bf8`.
- **2026-09-02**: Closed out the design-token pass (`a1fdd60`) and the TripMap off-system colors it had flagged but deferred. Live-verified `a1fdd60` against production (single-session Playwright pass, Journal/Packing/Currency/Translate/Feedback × 375/768/1280px): 0 findings, confirmed clean. Separately fixed TripMap.tsx's remaining literal-hex UI chrome (`#1D4E89`/`#163F73` filter button, focus ring, spinner → `var(--explorer-blue)`/`var(--amber)`; `#ef4444` delete button and search pin → `var(--ios-red)`; `#3b82f6` "Add to Trip" popup button → `var(--explorer-blue)`) — left the 8-color activity-type/day-route category legend and the native Leaflet popup's white-chrome neutrals (`#333`/`#666`/`#eee`) alone, both correctly out of scope (see "Design system" above). The TripMap Add-Tag time-collision fix's live click-through still isn't directly proven — repeated verification attempts this round burned through production's 5-req/15min auth rate limit before completing the two-pin click flow; the fix itself is unchanged and remains typecheck/build/test-clean. Commit `875efbc`.
- **2026-09-02**: Location-aware search suggestions added across the app's place pickers (ActivityFormDialog, TripPlanner origin/destination, Emergency, OfflineMaps explore) — debounced dropdown ranked toward the user's real location, or the trip's destination city when adding an activity inside a trip. New shared `usePlaceSuggestions`/`useUserLocation`/`useTripDestinationCoords` hooks + `PlaceSearchDropdown` component, backed by `&location=&radius=` bias params on the server's `/api/v1/places/search` (Google Places Text Search location-biasing — re-ranks, doesn't restrict). Weather kept its pre-existing keyboard-navigable dropdown (backed by `/api/v1/geocode`, a different endpoint with no bias param) and got a client-side distance sort instead of a rewrite. Landing's hero destination search deliberately kept unbiased — "where do you want to go" has no sensible near-me ranking. Same session also fixed a live user-reported bug: TripMap's "Add Tag" flow (map pin → itinerary activity) hardcoded every new activity's time to 10:00 AM, so two pins added back-to-back in the same session collided and got flagged by the itinerary's own chronology validator — new shared `nextAvailableTime()` helper (`client/src/lib/time.ts`) stacks the new pin after the day's last-scheduled activity instead. Typecheck/build/test (76/76) clean, bias endpoint live-confirmed in production via direct API call; full click-through UI verification of the Add-Tag flow was attempted live but blocked by self-inflicted rate-limiting from repeated QA signups in a short window, not a functional issue — worth a follow-up live pass if anything looks off. Commits `1dcec28`, `f40c677`.
- **2026-09-02**: Fresh 5-agent senior-engineer audit (backend, frontend, security, data/test/DevOps, design) found ~20 real, independently-verified issues, fixed across several commits: `reorderItinerary`/`updateTrip` concurrency and mass-assignment gaps, WebSocket auth skipping session revocation, unbounded Atlas conversation array, timing-unsafe admin-secret comparison, a hardcoded personal email in source, `User.email` unique index (TOCTOU signup race), missing Feedback/Notification indexes, `db:seed`/`db:clear` production guards, `/uploads` served with zero auth (journal photos + feedback screenshots — now authenticated/ownership-checked proxy routes), an avatar-upload JSON-string bypass of file validation, and a real shared-rate-limiter singleton bug that was randomly failing different tests in CI (express-rate-limit's in-memory store is shared across every test file in one vitest worker — now multiplied 100x in NODE_ENV=test). Frontend: wrong currency symbol on TripDetail, a missing `broadcastMutation` in `packingListToolHandler.ts` (Atlas-driven packing changes weren't live-refreshing — the real bug behind what looked like an `agentStore.ts` no-op), a travel-style case-mismatch, plus several smaller hygiene fixes (raw fetch → apiRequest, hard-reload → wouter, debug JSON panel gated to dev-only). One "critical" (wrong Groq model id) and 3 more findings (self-toast-on-own-edit, `staleTime:Infinity`-blocks-journal-refresh, a `tripStatus.ts`-class dead-code claim) were investigated and confirmed false positives — noted per-commit rather than silently dropped. Design pass (~30 raw-Tailwind-palette/shadow-elevation findings across Journal/Packing/CurrencyConverter/Translator/Feedback/RecapCard) and larger data/test items (account-deletion test coverage, CI AI-provider mocking, `itinerary: Mixed` schema, `UserMemoryModel` relocation) reported but not yet started — flagged to the user as a separate scope. All fixes typechecked/built/tested (76/76, repeatedly) and several live-verified against dev DB. Commits `16ad0d5` through `c9e4d68`.
- **2026-09-02**: Dead-code sweep (2 dispatched agents, findings independently re-verified — one candidate each was actually wrong: `client/src/utils/tripStatus.ts`+`TripCard.tsx`+`TripStatusBadge.tsx` and `PlannerWizard.tsx` all have real test coverage under `tests/frontend/`, and `server/services/agents/utils.ts` is genuinely imported via relative `./utils` by 3 agent files — none of those were touched). Removed 21 dead client files (WeatherCard.tsx, AgentHistoryList.tsx, DashboardFilters.tsx, AuthModal.tsx, hooks/useSidebar.ts, styles/tokens.ts, an empty trips/new.tsx, a shadowed client/src/tests/setup.ts, and 13 never-wired shadcn/ui scaffold components) and 7 dead server files (a root-level duplicate `agent.routes.ts`, a fully-unreachable `atlas.routes.ts` route tree mounted but never called by the client, `ReasoningEngine.ts` — all-stub, `middleware/rateLimit.ts` — dead sibling of `rateLimit.middleware.ts`, `prompts/packingPrompt.ts`, `schemas/journal.schemas.ts`). Also fixed a real bug found alongside: `AtlasConversationModel` was defined twice (shared/schema.ts, the real one, and a duplicate in server/models/AtlasConversation.ts that only `agent/memory.ts` used) — a "cannot overwrite model" hazard waiting for the wrong import order; repointed memory.ts to the canonical one. Live-verified: typecheck/build/test (76/76) clean, plus a real local server boot confirming route wiring. Commits `3eb49f4`, `ae7566a`.
- **2026-09-02**: `scripts/atlas-golden-eval.ts` never deleted the throwaway `golden_eval_*@example.com` account it signs up each run — 9 had piled up in production before a QA-account sweep caught it. Now wrapped in try/finally so cleanup runs regardless of pass/fail/throw. Live-verified: 12/13 golden cases passed, account confirmed deleted. Commit `ea6af5a`.
- **2026-09-01**: Senior-QA desktop full-journey pass against production (real Playwright, signup through account deletion) found 5 real bugs, most severe: `planTrip` (AiUtilitiesService.ts) cached a plan with empty-activity days (Zod's shape check allows `activities: []`) and served it to every user with matching destination/days/style/etc until the cache TTL expired — live-confirmed as a 7-day Tokyo trip showing "0 Activities" on all 7 days. Now rejects and never caches an empty-day plan. Also fixed: a raw `Open-Meteo API error: 429` tool error got paraphrased by Atlas into "rate limit exceeded," exposing an internal provider name (error text now user-appropriate at the source, plus a systemPrompt rule against ever echoing raw tool errors); the Maps page's initial world-view zoom looked like "unreadable black continents" under the dark-mode tile filter during the ~5s geolocation-resolution window (added a loading overlay); "Entry Fee ($)" was hardcoded regardless of trip currency (extracted `lib/currency.ts`, used in both `ActivityFormDialog` and `TripDetail`); Planner's Group Size validation error was a passing toast with no persistent field indicator. Commit `76bfb29`.
- **2026-09-01**: Senior-QA mobile pass against production (real Playwright, real signup, 375/430/768px) found 4 real bugs, most severe: `authStore.ts checkSession` treated a 429 on its retry as "keep session alive," but that branch only works if `isAuthenticated` was already true — on a fresh page load it starts false, so a rate-limit burst (live-reproduced) bounced a genuinely logged-in user to `/signin` on reload. Now retries up to 3x with backoff, covers 5xx/network too. Also fixed: the Atlas FAB visually sat on top of the Profile email field and the Planner's Group Size dropdown at 375-430px (extended the existing maps-page FAB-exclusion pattern to `/app/profile` and `/app/planner`); the `EmergencyServices.tsx` card header (title + country badge + Share button) could wrap ugly at 375px, fixed with proper flex-wrap; Leaflet's attribution control rendered invisible underneath the mobile bottom nav on the Maps page. Commits `2d7841b`, `df14d8f`.
- **2026-09-01**: `scripts/atlas-golden-eval.ts` CI run flagged 2 failures — Atlas answered correctly but skipped the required tool. `get_emergency_info` only ever returned physical hospital/police-station addresses (Places API), never a national dial code, so the model reasonably judged it useless for "what's the emergency number" and answered from memory instead — added a static ~40-country dial-code table as the tool's first result. `generate_packing_list` was never in `systemPrompt.ts`'s MUST-call-tool list — added it. Commit `d07940a`. **Live-verified against production** (real signup, real chat calls): emergency now calls `get_emergency_info` with correct numbers; packing now calls both `get_weather` and `generate_packing_list`. Also found and fixed a wrong doc'd route while verifying (`DELETE /auth/user` → real route is `/auth/delete-account`, commit `7a96fd1`). CI golden-eval itself not yet re-run to confirm 13/13 — the chat-level behavior is confirmed but the eval script hasn't been re-triggered.
- **2026-09-01**: Fresh 2-agent security audit found 5 more real vulnerabilities: `validate.ts` not stripping unknown keys (app-wide), `updateExpense`/`updateActivity` unconstrained `$set` field injection, `createPackingList` IDOR (attach a list to any trip), `updatePackingList` ownership-hijack via `Object.assign`, `UserMemoryModel` missing from account-deletion cascade. All fixed and live-verified against dev DB. Commit `d638cf9`.
- **2026-09-01**: Fixed collaborative-edit race condition on Trip itinerary/expenses (see "Non-obvious facts" above). Live-verified against dev MongoDB: 10 concurrent writes, no lost updates, no duplicate day entries. Commit `9144e4b`.
- **2026-08-31**: Brought README/ARCHITECTURE/AGENT_PROMPTS/API/PROJECT_RULES docs up to date with real system (was describing a fictional 4-stage agent pipeline, Groq-only LLM claim, `DATABASE_URL` instead of `MONGODB_URI`, disk-based uploads). Commit `f33adc2`.
- **2026-08-31**: Found and fixed a live `GOOGLE_API_KEY` leak in `trips.controller.ts` (`fetchImageForTrip`, `mapGooglePlace`) during production verification — 2 more instances of a pattern already fixed once elsewhere. Migrated 3 already-affected production trips. Commit `48dd1e3`.
- **2026-08-31**: Fixed hero search bar text truncation on narrow phones (icon-only "Plan Trip" button below `sm:`). Commit `afdafca`.
- **Earlier same session**: 3-round security/correctness audit (15 findings: userId type mismatches, 4x API key leak pattern, CSRF gaps, plaintext token storage, incomplete account-deletion cascade, forged-token session revocation, cross-tenant journal-tool gap, billing-abuse rate-limit gap). Landing.tsx full dark redesign + scroll/click motion pass. Gemini model-deprecation fix (`gemini-2.0-flash-lite` → `gemini-3.5-flash-lite`). CartoDB → OpenStreetMap maps migration.
